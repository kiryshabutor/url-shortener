# URL Shortener

Сервис для сокращения URL-адресов, написанный на Go. Позволяет создавать короткие ссылки для длинных URL и перенаправлять пользователей на оригинальные адреса.

## Возможности

- 🔗 Создание коротких ссылок из длинных URL
- 🎯 Автоматическая генерация алиаса или использование пользовательского
- 🔐 Защита API с помощью Basic Authentication
- 📊 Хранение данных в SQLite
- 🚀 Быстрое перенаправление на оригинальные URL
- 🗑️ Удаление коротких ссылок
- 📝 Структурированное логирование
- ⚙️ Гибкая конфигурация через YAML

## Технологии

- **Go 1.24.5** - основной язык программирования
- **Chi Router** - HTTP роутер
- **SQLite** - база данных
- **cleanenv** - загрузка конфигурации
- **slog** - структурированное логирование

## Установка

### Требования

- Go 1.24.5 или выше
- SQLite3

## Конфигурация

Создайте файл конфигурации (например, `config/local.yaml`):

```yaml
env: "local" # local, dev, prod
storage_path: "./storage/storage.db"
http_server:
  address: "localhost:8082"
  timeout: 4s # время на чтение запроса и отправку ответа
  idle_timeout: 60s # время жизни соединения с клиентом
  user: "myuser"
  password: "mypass"
```

### Переменные окружения

Вы можете переопределить некоторые параметры через переменные окружения:

- `CONFIG_PATH` - путь к файлу конфигурации (обязательно)
- `HTTP_SERVER_PASSWORD` - пароль для Basic Auth

## Запуск

```bash
# Установите путь к конфигурации
export CONFIG_PATH=./config/local.yaml

# Запустите сервер
./url-shortener
```

Или напрямую через `go run`:

```bash
export CONFIG_PATH=./config/local.yaml
go run ./cmd/url-shortener
```

## API

### Создание короткой ссылки

**POST** `/url`

Требует Basic Authentication.

**Запрос:**
```json
{
  "url": "https://example.com/very/long/url",
  "alias": "example" // опционально, если не указан - будет сгенерирован автоматически
}
```

**Ответ (успех):**
```json
{
  "status": "OK",
  "alias": "example"
}
```

**Пример с curl:**
```bash
curl -X POST http://localhost:8082/url \
  -u myuser:mypass \
  -H "Content-Type: application/json" \
  -d '{"url": "https://example.com/very/long/url", "alias": "example"}'
```

### Перенаправление по короткой ссылке

**GET** `/{alias}`

Публичный endpoint, не требует аутентификации.

**Пример:**
```bash
curl -L http://localhost:8082/example
```

Или просто откройте в браузере: `http://localhost:8082/example`

### Удаление короткой ссылки

**DELETE** `/url/{alias}`

Требует Basic Authentication.

**Пример с curl:**
```bash
curl -X DELETE http://localhost:8082/url/example \
  -u myuser:mypass
```

**Ответ (успех):**
```json
{
  "status": "OK"
}
```

## Структура проекта

```
url-shortener/
├── cmd/
│   └── url-shortener/
│       └── main.go              # Точка входа приложения
├── config/
│   └── local.yaml               # Файл конфигурации
├── internal/
│   ├── config/
│   │   └── config.go           # Структуры конфигурации
│   ├── http-server/
│   │   └── handlers/
│   │       ├── redirect/       # Обработчик перенаправления
│   │       └── url/
│   │           ├── save/       # Обработчик создания URL
│   │           └── remove/     # Обработчик удаления URL
│   ├── lib/
│   │   ├── api/response/       # Утилиты для ответов API
│   │   ├── logger/             # Логирование
│   │   └── random/             # Генерация случайных строк
│   └── storage/
│       ├── storage.go          # Интерфейсы хранилища
│       └── sqlite/             # Реализация SQLite
├── storage/
│   └── storage.db              # База данных SQLite
├── go.mod
└── go.sum
```

## Окружения

Проект поддерживает три окружения:

- **local** - красивое логирование в консоль, уровень DEBUG
- **dev** - JSON логирование, уровень DEBUG
- **prod** - JSON логирование, уровень INFO

Окружение задается в файле конфигурации через параметр `env`.

## База данных

При первом запуске автоматически создается таблица `url` со следующей структурой:

```sql
CREATE TABLE url(
    id INTEGER PRIMARY KEY,
    alias TEXT NOT NULL UNIQUE,
    url TEXT NOT NULL
);

CREATE INDEX idx_alias ON url(alias);
```

## Безопасность

- API endpoints для создания и удаления URL защищены Basic Authentication
- Пароль можно задать через переменную окружения `HTTP_SERVER_PASSWORD`
- Endpoint для перенаправления публичный и не требует аутентификации

## Разработка

### Запуск в режиме разработки

```bash
export CONFIG_PATH=./config/local.yaml
go run ./cmd/url-shortener
```

### Тестирование

```bash
# Создание короткой ссылки
curl -X POST http://localhost:8082/url \
  -u myuser:mypass \
  -H "Content-Type: application/json" \
  -d '{"url": "https://www.google.com"}'

# Использование короткой ссылки
curl -L http://localhost:8082/<полученный_alias>

# Удаление ссылки
curl -X DELETE http://localhost:8082/url/<alias> \
  -u myuser:mypass
```

